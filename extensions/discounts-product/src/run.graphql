query RunInput {
  shop {
    metafield(namespace: "$app:discounts-allocator", key: "function-configuration") {
      value
    }
  }
  cart {
    lines {
      id              # ✅ 加上这个！
      quantity
      cost {
        totalAmount {
          amount
        }
      }
      merchandise {
        ... on ProductVariant {
          id
          product {
            id
          }
        }
      }
    }
    cost {
      subtotalAmount {
        amount
      }
    }
  }
}
